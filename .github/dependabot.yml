version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: monthly
    open-pull-requests-limit: 3
    # @cloudflare/vitest-pool-workers declares a peer range of vitest ^4.1.0;
    # a major bump fails CI until the pool supports it.
    ignore:
      - dependency-name: "vitest"
        update-types: ["version-update:semver-major"]
    groups:
      toolchain:
        patterns: ["*"]
  # Actions are pinned by digest; this is what moves the pins.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    groups:
      actions:
        patterns: ["*"]
